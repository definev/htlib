import 'package:flutter/material.dart';

class LibraryDiagram extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Sơ đồ thư viện"),
      ),
    );
  }
}
