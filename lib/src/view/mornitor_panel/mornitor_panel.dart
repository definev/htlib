import 'package:flutter/material.dart';

class MornitorPanel extends StatelessWidget {
  const MornitorPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
